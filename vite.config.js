import { defineConfig } from 'vite';
import path from "path";

export default defineConfig({
  // base: '/club/',
  // css: {
  //   preprocessorOptions: {
  //     scss: {
  //       additionalData: `@use "${path
  //         .resolve(__dirname, 'src/style/mixins.scss')
  //         .replace(/\\/g, '/')}" as *;`
  //     }
  //   }
  // }
});